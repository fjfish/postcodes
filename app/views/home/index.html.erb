<h1>Home</h1>


<p>
  First set up an LSOA matcher, then test it using the postocde query form
</p>

<table>
  <% if LsoaMatcher.count > 0 %>
    <tr>
      <td><%= link_to "Postcode query", postcode_query_index_path %></td>
    </tr>
  <% end %>
  <tr>
    <td><%= link_to "LSOA Matchers", lsoa_matchers_path %></td>
  </tr>
</table>
